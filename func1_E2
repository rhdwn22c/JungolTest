#include <stdio.h>

int f(int num) {

	int pp = 1;
	for (int i = 0; i < num; i++) {
		for (int j = 0; j < num; j++) {
			printf("%d ", pp);
			pp = pp + i + 1;
		}
		pp = i + 2;
		printf("\n");
	}
}

int main() {
	
	int num;

	scanf("%d", &num);
	
	f(num);
	
}
