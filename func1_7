#include <stdio.h>

void f(int a, int b) {
	if (a > b) {
		printf("%d %d", a / 2, b * 2);
	}
	else if (a < b) {
		printf("%d %d", a * 2, b / 2);
	}
}

int main() {
	
	int a, b;
	scanf("%d %d", &a, &b);
	f(a, b);
	
}
