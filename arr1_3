#include <stdio.h>

int main() {
	char arr[10];

	for (int i = 0; i < 10; i++) {
		scanf(" %c", &arr[i]);
	}
	for (int i = 0; i < 10; i++) {
		if (i == 0 || i == 3 || i == 6) {
			printf("%c ", arr[i]);
		}
	}
}
