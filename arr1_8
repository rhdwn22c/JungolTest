#include <stdio.h>

int main() {
	int arr[10];
	int sum = 0;
	double avg = 0;
	for (int i = 0; i < 10; i++) {
		scanf("%d", &arr[i]);
		if (i % 2 == 1) {
			sum += arr[i];
		}
		else if (i % 2 == 0 || i == 0) {
			avg += (double)arr[i];
		}
	}
	printf("sum : %d\navg : %.1lf", sum, avg / 5);
}
