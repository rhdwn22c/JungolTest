#include <stdio.h>

int main() {
	int arr[4][3];
	int sum[4] = { 0, };
	for (int i = 0; i < 4;i++) {
		for (int j = 0; j < 3; j++) {
			scanf("%d", &arr[i][j]);
			sum[i] += arr[i][j];
		}
	}
	for (int i = 0; i < 4; i++) {
		printf("%dclass : %d\n", i + 1, sum[i]);
	}
	
}

