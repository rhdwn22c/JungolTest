#include <stdio.h>

int main() {
	int dice;
	int arr[6] = { 0, };

	for (int i = 0; i < 10; i++) {
		scanf("%d", &dice);
		arr[dice - 1]++;
	}
	for (int i = 0; i < 6; i++) {
		printf("%d : %d\n", i + 1, arr[i]);
	}
}
