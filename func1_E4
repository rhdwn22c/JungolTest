#include <stdio.h>

int f(int a, int b) {
	int sub = 0;
	if (a > b) {
		sub = a * a - b * b;
		printf("%d", sub);
	}
	else if (b > a) {
		sub = b * b - a * a;
		printf("%d", sub);
	}
}


int main() {
	
	int a, b;

	scanf("%d %d", &a, &b);
	
	f(a, b);
	
}
