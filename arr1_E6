#include <stdio.h>

int main() {
	char arr[6] = { 'J', 'U', 'N', 'G', 'O', 'L' };
	char alp;

	scanf("%c", &alp);

	for (int i = 0; i < 6;i++) {
		if (alp == arr[i]) {
			printf("%d", i);
			return 0;
		}
	}
	printf("none");
}
